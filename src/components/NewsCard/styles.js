import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  containerCard: {
    width: '100%',
    marginRight: '30px',
    marginTop: '30px',
  },
}));

export default useStyles;
